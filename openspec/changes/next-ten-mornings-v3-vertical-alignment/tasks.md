## 1. OpenSpec

- [x] 1.1 Create a focused v3 alignment change and validate it in strict mode.

## 2. Row Metrics

- [x] 2.1 Replace balanced outer lanes with v3-style date lane, minimum gap, centered tag lane, minimum gap, and trailing lane metrics.
- [x] 2.2 Update focused row-metrics tests for shared middle-lane center and protected two-tag capacity.

## 3. SwiftUI Layout

- [x] 3.1 Center-align the outer row date/tag/time elements instead of using first-baseline alignment.
- [x] 3.2 Keep the time lockup's internal main-time/meridiem baseline relationship while centering the whole lockup in the row.
- [x] 3.3 Apply forecast-local compact chip spacing and padding.

## 4. Validation

- [x] 4.1 Run OpenSpec strict validation, whitespace checks, focused Xcode tests, and full Xcode validation.
- [x] 4.2 Commit and push the completed change to `main`.
